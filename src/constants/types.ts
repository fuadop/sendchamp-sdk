export enum SendchampStatus {
  "success" = "success",
  "error" = "error",
}
export enum SendchampMode {
  "test" = "test",
  "live" = "live",
  "local-simulator" = "local-simulator",
}
export enum Channel {
  "sms" = "sms",
  "email" = "email",
  "whatsapp" = "whatsapp",
  "voice" = "voice",
}

export enum TokenType {
  "numeric" = "numeric",
  "alphanumeric" = "alphanumeric",
}

export enum InsightType {
  "basic" = "basic",
  "core" = "core",
  "standard" = "standard",
}

export enum WhatsAppType {
  "template" = "template",
  "video" = "video",
  "audio" = "audio",
  "location" = "location",
  "sticker" = "sticker",
  "text" = "text",
}

export enum SenderUseCase {
  "transactional" = "Transactional",
  "marketting" = "Marketing",
  "transactional & marketing" = "Transactional & Marketing",
}

export enum VoiceType {
  "outgoing" = "outgoing",
  "incoming" = "incomming",
  "template" = "template",
}
